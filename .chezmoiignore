# Ignore files that shouldn't be managed by chezmoi
# or where existing configs should be preserved

# Preserve existing neovim config (LazyVim)
.config/nvim/**

# Don't deploy bootstrap scripts to home directory
scripts/**

# BUT do manage .config/scripts (user scripts)

# Preserve README in home (if any)
README.md

# Don't manage manually maintained secrets file (Linux only)
.config/shell/secrets.env
